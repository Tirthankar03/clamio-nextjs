import { LucideIcon } from "lucide-react";

export type SalesProps = {
    name: string,
    email: string,
    saleAmount: string,
 }


 export type CardProps = {
    label: string;
    icon: LucideIcon;
    amount: string;
    discription: string;
  };
